import React from 'react';

const InterfaceLoadSpinner = () => {
    return (
        <div className="interface-spinner">
            <img className="spinner-gif" src={discordSpinner} />
        </div>
    );
};

export default InterfaceLoadSpinner;